ChatGPT (Bypass) — программа для обхода блокировок ChatGPT в России.
Работает через автоматическую смену DNS на публичные (Google/Cloudflare) и фильтрацию трафика с помощью WinDivert, минимально корректируя пакеты для обхода DPI.

Цель: обойти сложную систему DNS-преобразования простыми методами. Почти получилось...

Возможности

Автоматическая смена DNS:

8.8.8.8 (Google)

1.1.1.1 (Cloudflare)

Обход DPI через библиотеку WinDivert

Запуск всего в один клик

Установка зависимостей:

pip install pydivert

Запуск:

Через собранный лаунчер: My project\AI\GPT\dist\launcher

Или напрямую: python My project\AI\GPT\launcher.py


Известные проблемы:

При запуске программа может ломать интернет-соединение.